fix: v1.9.7 — heal NaN-polluted challenge state, summon works again

- BUG: summon panel showed NaN/undefined attempts & forever failed when
  challenge.todayUsed became NaN (missing field from old data/sync →
  undefined+1=NaN → rate NaN → roll<NaN always false)
- getChallenge() now normalizes every field: NaN/undefined/missing →
  defaults (todayUsed 0, seasonBonus {}, weekDays [], booleans coerced)
- checkChallengeDailyReset() actively repairs + persists bad values
- canSummon/attemptSummon/renderSummonPanel: isFinite guards on
  rate/used/total/volume
- existing polluted data self-heals on next page open (no user action)
- bump v1.9.7 + cache v13, changelog + README synced