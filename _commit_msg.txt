fix: v1.9.8 — ?/? summon panel root cause (used-up state), screen debug panel

- ROOT CAUSE: canSummon returns {can:false} WITHOUT total/used fields when
  today's attempts are exhausted; renderSummonPanel never checked info.can
  and rendered a summonable-looking panel with undefined total → "?/? 次"
- FIX: canSummon can:false branches now carry total/used/rate/vol;
  renderSummonPanel gained a !info.can branch showing the reason card
  (次数用完 + today volume + used/remaining) instead of the summon UI
- DEBUG: tap summon card title 3x to toggle an on-screen debug panel
  (canSummon JSON / volume / today entries detail / challenge state),
  temporary for mobile diagnosis, will remove after confirmation
- bump v1.9.8 + cache v14, changelog + README synced